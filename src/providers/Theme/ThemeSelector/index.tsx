'use client'

import React from 'react'

// This component has been simplified to remove dark mode support
// It now renders nothing as we only support light mode
export const ThemeSelector: React.FC = () => {
  return null
}
