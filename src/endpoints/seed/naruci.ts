import type { Media } from '@/payload-types'

export const naruci: Omit<Media, 'createdAt' | 'id' | 'updatedAt'> = {
  alt: 'Matka držící kojence v náručí, symbol péče o novorozence, lásky a podpory.',
}
